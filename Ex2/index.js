// TODO: Declare global variable to hold the countdown interval

// TODO: Select the element for displaying the remaining time

// TODO: Select the element for displaying the end time

// TODO: Select all buttons with a data-time attribute

// TODO: Define function to start the countdown timer
function timer(seconds) {
  // Clear any existing timers

  // Calculate the time when the timer should end

  // Display the initial time left and end time

  // Update the display every second
  countdown = setInterval(() => {
    // Calculate the remaining seconds

    // Check if we should stop the timer
    if (secondsLeft < 0) {

    }

    // Update the display with the time left

  }, 1000);
}

// TODO: Define function to display the time left
function displayTimeLeft(seconds) {
  
}

// TODO: Define function to display the end time
function displayEndTime(timestamp) {
  
    // Display the end time
}

// TODO: Define function to start the timer when a button is clicked
function startTimer() {
  c
}

// TODO: Add event listeners for button clicks to start the timer

// TODO: Add event listener for form submission to start a custom timer
document.customForm.addEventListener('submit', function(e) {
  
});
